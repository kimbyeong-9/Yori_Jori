DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:55432/yorijori_test

# Gemini API (4단계 — 추천 로직)
# 실제 키는 .env(커밋 안 함)에만 넣는다. 로그/에러 메시지에 절대 노출하지 않는다.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
